services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: azizaid_hub
      POSTGRES_USER: azizaid
      POSTGRES_PASSWORD: azizaid_dev
    ports:
      - "5433:5432"
    volumes:
      - azizaid-db-data:/var/lib/postgresql/data
      - ./db/01_schema.sql:/docker-entrypoint-initdb.d/01_schema.sql
      - ./db/02_seed.sql:/docker-entrypoint-initdb.d/02_seed.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U azizaid -d azizaid_hub"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: .
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_URL: jdbc:postgresql://db:5432/azizaid_hub
      DB_USERNAME: azizaid
      DB_PASSWORD: azizaid_dev
      JWT_SECRET: docker-compose-dev-secret-nao-usar-em-producao
    ports:
      - "8081:8081"

volumes:
  azizaid-db-data:
